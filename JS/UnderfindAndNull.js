let mensagem = 'Olá Impressionador';
let preco = 50.0;
let logado = true;

// Undefined - nâo atribuido valor ou definido de forma incorreta
let produto;
let carrinho = undefined;
console.log(typeof produto);
console.log(carrinho);

// Null - Possui um valor (nulo/vazio)
let lista = null;
console.log(lista);
console.log(typeof lista);
